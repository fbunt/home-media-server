# Example env file for ntfy-send.sh (files/usr/libexec/mediaserver/ntfy-send.sh).
#
# Copy this to /etc/mediaserver/ntfy.env on the host (NOT committed to git,
# NOT baked into the image) and fill in real values.
NTFY_SERVER=https://ntfy.sh
NTFY_TOPIC=CHANGE_ME
